import React from "react";

const CurrentStop = ({ paintingId }) => {
  /* Mising:
  - Get painting info from api
  - Return name, image */
  return (
    <div>
      <p>Current journey stop: {paintingId}</p>
      <p>Name:</p>
    </div>
  );
};

export default CurrentStop;
